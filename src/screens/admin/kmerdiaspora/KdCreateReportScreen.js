import React,{useState}from'react';import{ScrollView,Alert}from'react-native';import Input from'../../../components/Input';import SelectField from'../../../components/SelectField';import Button from'../../../components/Button';import Card from'../../../components/Card';import{createReport}from'../../../services/kmerDiasporaService';import{Screen,styles}from'../AdminUI';export default function KdCreateReportScreen({navigation}){const[type,setType]=useState('community');const[title,setTitle]=useState('');const[content,setContent]=useState('');const[loading,setLoading]=useState(false);const types=['community','job_requests','driver_requests','matching','quests','moderation','custom'];const submit=async()=>{try{setLoading(true);await createReport({reportType:type,title,content});Alert.alert('Rapport créé','Le rapport communautaire a été enregistré.');navigation.goBack()}catch(e){Alert.alert('Erreur',e.message)}finally{setLoading(false)}};return <Screen title="Créer un rapport"><ScrollView contentContainerStyle={styles.scroll}><Card><SelectField label="Type" valueLabel={type} onPress={()=>setType(types[(types.indexOf(type)+1)%types.length])}/><Input label="Titre" value={title} onChangeText={setTitle} placeholder="Titre du rapport" autoCapitalize="sentences"/><Input label="Contenu" value={content} onChangeText={setContent} placeholder="Contenu" autoCapitalize="sentences"/><Button title="Créer le rapport" loading={loading} onPress={submit}/></Card></ScrollView></Screen>}
