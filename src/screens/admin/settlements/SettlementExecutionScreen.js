import React,{useState}from'react';import{ScrollView,Alert}from'react-native';import Input from'../../../components/Input';import Button from'../../../components/Button';import Card from'../../../components/Card';import{executeSettlement}from'../../../services/settlementService';import{Screen,styles}from'../AdminUI';export default function SettlementExecutionScreen({route,navigation}){const{settlementId}=route.params;const[ref,setRef]=useState('');const[loading,setLoading]=useState(false);const submit=async()=>{try{setLoading(true);await executeSettlement({settlementId,externalReference:ref});Alert.alert('Exécuté','Le règlement est maintenant en traitement.');navigation.goBack()}catch(e){Alert.alert('Erreur',e.message)}finally{setLoading(false)}};return <Screen title="Exécuter le règlement"><ScrollView contentContainerStyle={styles.scroll}><Card><Input label="Référence bancaire" value={ref} onChangeText={setRef} placeholder="Référence de virement"/><Button title="Confirmer l'exécution" loading={loading} onPress={submit}/></Card></ScrollView></Screen>}
