import React,{useState} from 'react';import {ScrollView,Alert} from 'react-native';import Input from '../../../components/Input';import SelectField from '../../../components/SelectField';import Button from '../../../components/Button';import Card from '../../../components/Card';import {adjustWallet} from '../../../services/adminService';import {Screen,styles} from '../AdminUI';
export default function WalletAdjustmentScreen({route,navigation}){const{userId}=route.params;const[amount,setAmount]=useState('');const[type,setType]=useState('credit');const[reason,setReason]=useState('');const[loading,setLoading]=useState(false);const submit=async()=>{try{setLoading(true);await adjustWallet({userId,amount,entryType:type,reason});Alert.alert('Opération effectuée','Le wallet a été mis à jour.');navigation.goBack()}catch(e){Alert.alert('Erreur',e.message)}finally{setLoading(false)}};return <Screen title="Ajuster le wallet"><ScrollView contentContainerStyle={styles.scroll}><Card><SelectField label="Type" valueLabel={type==='credit'?'Crédit':'Débit'} onPress={()=>setType(type==='credit'?'debit':'credit')}/><Input label="Montant" value={amount} onChangeText={setAmount} placeholder="0" keyboardType="numeric"/><Input label="Motif" value={reason} onChangeText={setReason} placeholder="Motif de l'ajustement" autoCapitalize="sentences"/><Button title={type==='credit'?'Créditer le wallet':'Débiter le wallet'} loading={loading} onPress={submit} variant={type==='credit'?'success':'danger'}/></Card></ScrollView></Screen>}
