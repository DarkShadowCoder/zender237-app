import React,{useState} from 'react';import {ScrollView,Text,Pressable,Alert} from 'react-native';import * as ImagePicker from 'expo-image-picker';import {Ionicons} from '@expo/vector-icons';import {colors,typography,spacing,radii} from '../../../theme/theme';import Button from '../../../components/Button';import Input from '../../../components/Input';import {uploadSettlementProof} from '../../../services/settlementService';
export default function PartnerSettlementProofScreen({route,navigation}){const id=route?.params?.settlementId;const [uri,setUri]=useState(null),[desc,setDesc]=useState(''),[loading,setLoading]=useState(false);const pick=async()=>{const r=await ImagePicker.launchImageLibraryAsync({mediaTypes:ImagePicker.MediaTypeOptions.Images,quality:.85});if(!r.canceled)setUri(r.assets[0].uri)};const upload=async()=>{try{if(!uri)throw new Error('Sélectionnez une preuve.');setLoading(true);await uploadSettlementProof({settlementId:id,uri,fileName:`settlement-${Date.now()}.jpg`,contentType:'image/jpeg',description:desc});navigation.goBack()}catch(e){Alert.alert('Erreur',e.message)}finally{setLoading(false)}};return <ScrollView contentContainerStyle={{padding:spacing.screenHorizontal,paddingTop:spacing.screenVertical,paddingBottom:60}}><Text style={typography.h1}>Preuve du règlement</Text><Pressable onPress={pick} style={{height:220,borderWidth:1,borderStyle:'dashed',borderColor:colors.border.default,borderRadius:radii.xl,alignItems:'center',justifyContent:'center',marginTop:20,backgroundColor:colors.background.surface}}>{uri?<Ionicons name="checkmark-circle" size={58} color={colors.success.default}/>:<Ionicons name="cloud-upload-outline" size={48} color={colors.brand.primary}/>}<Text style={[typography.bodyBold,{marginTop:8}]}>{uri?'Preuve sélectionnée':'Ajouter le justificatif'}</Text></Pressable><Input label="Description" value={desc} onChangeText={setDesc} placeholder="Ex. reçu bancaire, capture…"/><Button title="Téléverser la preuve" loading={loading} onPress={upload}/></ScrollView>}
