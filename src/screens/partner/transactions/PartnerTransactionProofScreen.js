import React from 'react';
import {ScrollView,Text,View,Pressable,Alert} from 'react-native';
import * as ImagePicker from 'expo-image-picker';
import {Ionicons} from '@expo/vector-icons';
import {colors,typography,spacing,radii} from '../../../theme/theme';
import Button from '../../../components/Button';
import {uploadSettlementProof} from '../../../services/settlementService';
export default function PartnerTransactionProofScreen({route,navigation}){const tx=route?.params?.transactionId;const [uri,setUri]=React.useState(null);const [loading,setLoading]=React.useState(false);const pick=async()=>{const r=await ImagePicker.launchImageLibraryAsync({mediaTypes:ImagePicker.MediaTypeOptions.Images,quality:.85});if(!r.canceled)setUri(r.assets[0].uri)};return <ScrollView contentContainerStyle={{padding:spacing.screenHorizontal,paddingTop:spacing.screenVertical,paddingBottom:60}}><Text style={typography.h1}>Preuve de l'opération</Text><Text style={[typography.caption,{marginTop:4}]}>Sélectionnez le justificatif à associer au règlement.</Text><Pressable onPress={pick} style={{height:220,borderRadius:radii.xl,borderWidth:1,borderStyle:'dashed',borderColor:colors.border.default,marginTop:24,alignItems:'center',justifyContent:'center',backgroundColor:colors.background.surface}}>{uri?<><Ionicons name="checkmark-circle" size={56} color={colors.success.default}/><Text style={typography.bodyBold}>Image sélectionnée</Text></>:<><Ionicons name="cloud-upload-outline" size={48} color={colors.brand.primary}/><Text style={[typography.bodyBold,{marginTop:8}]}>Ajouter une preuve</Text><Text style={typography.caption}>JPG, PNG ou capture d'écran</Text></>}</Pressable><Button title="Continuer vers le règlement" onPress={()=>navigation.navigate('PartnerSettlements',{transactionId:tx,proofUri:uri})} style={{marginTop:20}}/></ScrollView>}
